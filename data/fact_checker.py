def check_claim(claim: str):
    #uses keyword matching (e.g "COVID", "debt", "unemployment")
    #selects right dataset/query
    # returns structured answer (facts in plain Englis + source)
    return